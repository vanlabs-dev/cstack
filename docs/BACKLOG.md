# Backlog

Parked items, out of scope for the current sprint. Move into a sprint when prioritized.

- [ ] Add `.gitattributes` to enforce LF line endings cross-platform; current Windows
      checkout warnings are cosmetic but conflict with the `.editorconfig` LF setting.
