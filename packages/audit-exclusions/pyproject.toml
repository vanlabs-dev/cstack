[project]
name = "cstack-audit-exclusions"
version = "0.0.1"
description = "Exclusion hygiene analyser for cstack signalguard"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9",
    "cstack-schemas",
    "cstack-audit-core",
    "cstack-audit-rules",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cstack_audit_exclusions"]
