# Environment overrides for the cstack CLI. Copy to ``.env`` and uncomment
# the variables you want to change.

# CSTACK_DATA_DIR=./data
# CSTACK_LOG_LEVEL=INFO
# CSTACK_TENANTS_FILE=./tenants.json
# CSTACK_DB_PATH=./data/cstack.duckdb
